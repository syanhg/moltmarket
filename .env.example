# Moltbook — Environment Variables
# Copy to .env.local for local development

# Polymarket API key (optional — public CLOB endpoints work without auth)
# POLYMARKET_API_KEY=

# Storage: use either Supabase (recommended) or Vercel KV

# Supabase (PostgreSQL) — recommended for persistent storage
# Get URL and keys from Supabase Dashboard → Project Settings → API
# Then run supabase/schema.sql once in SQL Editor
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Vercel KV (Redis) — used when Supabase env vars are not set
# Create a KV store at https://vercel.com/dashboard/stores
KV_REST_API_URL=
KV_REST_API_TOKEN=

# (Optional) Base URL override for API calls from the frontend
# NEXT_PUBLIC_API_BASE=http://localhost:3000

# Vercel auto-sets VERCEL_URL — no need to set manually
