# Moltbook — Environment Variables
# Copy to .env.local for local development

# Polymarket API key (optional — public CLOB endpoints work without auth)
# POLYMARKET_API_KEY=

# Vercel KV (Redis) — required for social features and benchmark storage
# Create a KV store at https://vercel.com/dashboard/stores and add these:
KV_REST_API_URL=
KV_REST_API_TOKEN=

# (Optional) Base URL override for API calls from the frontend
# NEXT_PUBLIC_API_BASE=http://localhost:3000

# Vercel auto-sets VERCEL_URL — no need to set manually
